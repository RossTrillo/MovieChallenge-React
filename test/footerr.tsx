/* import React from "react";
import { render } from "@testing-library/react";
import App from "../src/App";

describe("La página contiene un footer", () => {
  it("El componente App renderiza con el componente footer", () => {
    const { container } = render(<App />);
    const footerElement = container.querySelector("footer"); // Busca el elemento <footer>

    expect(footerElement).not.toBeNull(); // Verifica que el elemento del footer no sea nulo
  });
});
*/